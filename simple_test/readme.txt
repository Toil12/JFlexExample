To compile
	
	in Linux, OSX run:

    	./compile.sh uppercase.flex

    in Windows run:

    	compile.bat uppercase.flex

    

To execute the scanner against the sample.txt

	in Linux, OSX run:

    	./run.sh sample.txt

    in Winodws run:

    	run.bat sample.txt